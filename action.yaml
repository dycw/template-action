name: abc
description: description

branding:
  icon: activity
  color: gray-dark

inputs:
  token:
    description: Personal access token (PAT)
    default: ${{github.token}}
  flag:
    description: Example flag
    default: false

runs:
  using: composite
  steps:
    - name: Check out repo
      uses: actions/checkout@v6
      with:
        token: ${{inputs.token}}

    - name: Install 'uv'
      uses: astral-sh/setup-uv@v7
      with:
        github-token: ${{inputs.token}}

    - name: Run 'action abc'
      env:
        FLAG: ${{inputs.flag}}
      run: uv tool run --from dycw-actions@latest --isolated --resolution highest --prerelease disallow action abc
      shell: bash
